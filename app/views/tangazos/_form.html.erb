<%= form_for(@tangazo) do |f| %>
  <% if @tangazo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tangazo.errors.count, "error") %> prohibited this tangazo from being saved:</h2>

      <ul>
      <% @tangazo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :owner %><br>
    <%= f.text_field :owner %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
